<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <title>Edit MCQ Question</title>
</head>
<body>
    <h1>Edit MCQ Question</h1>
    <form action="/admin/exam/<%= examId %>/edit/mcq/<%= mcq._id %>" method="POST">
        <label>Question:</label>
        <input type="text" name="question" value="<%= mcq.question %>" required>
        
        <label>Options:</label>
        <input type="text" name="options[0]" value="<%= mcq.options[0] %>" required>
        <input type="text" name="options[1]" value="<%= mcq.options[1] %>" required>
        <input type="text" name="options[2]" value="<%= mcq.options[2] %>" required>
        <input type="text" name="options[3]" value="<%= mcq.options[3] %>" required>

        <label>Correct Answer:</label>
        <input type="text" name="correctAnswer" value="<%= mcq.correctAnswer %>" required>

        <label>Marks:</label>
        <input type="number" name="marks" value="<%= mcq.marks %>" required>

        <button type="submit">Update MCQ</button>
    </form>

    <form action="/admin/exam/<%= examId %>/delete/mcq/<%= mcq._id %>" method="POST">
        <button type="submit" onclick="return confirm('Are you sure?')">Delete Question</button>
    </form>

    <a href="/admin/exam/questions/<%= examId %>">Back to Questions</a>
</body>
</html> -->



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit MCQ Question</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #141D2F; /* Dark blue background */
        }
        h1, h2 {
            color: #0056b3; /* Darker blue for headings */
        }
        .container {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 123, 255, 0.1);
            border: 3px solid #1D4ED8;
            max-width: 1200px;
            margin: 30px auto;
        }
        .form-label {
            font-weight: bold;
        }
        .form-control {
            margin-bottom: 15px;
            border: 1px solid #b3d1ff;
            border-radius: 5px;
        }
        .form-control:focus {
            border-color: #1D4ED8;
            box-shadow: 0 0 0 0.25rem rgba(29, 78, 216, 0.25);
        }
        .btn-primary {
            background: #0d6efd;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
        }
        .btn-primary:hover {
            background: #0056b3;
        }
        .btn-danger {
            background: #dc3545;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            margin-top: 15px;
        }
        .btn-danger:hover {
            background: #bb2d3b;
        }
        .back-btn {
            background: #6c757d;
            display: inline-block;
            padding: 8px 16px;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
        .back-btn:hover {
            background: #5a6268;
            color: white;
        }
        .card {
            border: 1px solid #b3d1ff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 123, 255, 0.05);
        }
        .section-header {
            background-color: #e6f0ff;
            padding: 10px 15px;
            border-radius: 5px;
            margin: 25px 0 15px 0;
            border-left: 5px solid #1D4ED8;
        }
    </style>
</head>
<body>

<div class="container">
    <h1 class="text-center mb-4">Edit MCQ Question</h1>

    <!-- Edit MCQ Form -->
    <div class="section-header">
        <h2 class="mb-0">Question Details</h2>
    </div>
    
    <div class="card p-4 mb-3">
        <form action="/admin/exam/<%= examId %>/edit/mcq/<%= mcq._id %>" method="POST">
            <div class="mb-3">
                <label for="question" class="form-label">Question:</label>
                <input type="text" id="question" name="question" class="form-control" value="<%= mcq.question %>" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Options:</label>
                <input type="text" name="options[0]" class="form-control mb-2" value="<%= mcq.options[0] %>" required>
                <input type="text" name="options[1]" class="form-control mb-2" value="<%= mcq.options[1] %>" required>
                <input type="text" name="options[2]" class="form-control mb-2" value="<%= mcq.options[2] %>" required>
                <input type="text" name="options[3]" class="form-control mb-2" value="<%= mcq.options[3] %>" required>
            </div>

            <div class="mb-3">
                <label for="correctAnswer" class="form-label">Correct Answer:</label>
                <input type="text" id="correctAnswer" name="correctAnswer" class="form-control" value="<%= mcq.correctAnswer %>" required>
            </div>

            <div class="mb-3">
                <label for="marks" class="form-label">Marks:</label>
                <input type="number" id="marks" name="marks" class="form-control" value="<%= mcq.marks %>" required>
            </div>

            <button type="submit" class="btn btn-primary">Update MCQ</button>
        </form>
    </div>

    <!-- Delete Form -->
    <div class="text-center">
        <form action="/admin/exam/<%= examId %>/delete/mcq/<%= mcq._id %>" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-danger" onclick="return confirm('Delete this question?')">🗑 Delete Question</button>
        </form>
    </div>

    <!-- Back Link -->
    <div class="text-center mt-4">
        <a href="/admin/exam/questions/<%= examId %>" class="back-btn">⬅ Back to Questions</a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
