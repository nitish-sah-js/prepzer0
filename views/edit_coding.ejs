<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Coding Question</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #141D2F; /* Dark blue background */
        }
        h1, h2 {
            color: #0056b3; /* Darker blue for headings */
        }
        .container {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 123, 255, 0.1);
            border: 3px solid #1D4ED8;
            max-width: 1200px;
            margin: 30px auto;
        }
        .form-label {
            font-weight: bold;
        }
        .form-control {
            margin-bottom: 15px;
            border: 1px solid #b3d1ff;
            border-radius: 5px;
        }
        .form-control:focus {
            border-color: #1D4ED8;
            box-shadow: 0 0 0 0.25rem rgba(29, 78, 216, 0.25);
        }
        textarea {
            resize: vertical;
            min-height: 100px;
        }
        .btn-primary {
            background: #0d6efd;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
        }
        .btn-primary:hover {
            background: #0056b3;
        }
        .btn-danger {
            background: #dc3545;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            margin-top: 15px;
        }
        .btn-danger:hover {
            background: #bb2d3b;
        }
        .back-btn {
            background: #6c757d;
            display: inline-block;
            padding: 8px 16px;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
        .back-btn:hover {
            background: #5a6268;
            color: white;
        }
        .card {
            border: 1px solid #b3d1ff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 123, 255, 0.05);
        }
        .section-header {
            background-color: #e6f0ff;
            padding: 10px 15px;
            border-radius: 5px;
            margin: 25px 0 15px 0;
            border-left: 5px solid #1D4ED8;
        }
    </style>
</head>
<body>

<div class="container">
    <h1 class="text-center mb-4">Edit Coding Question</h1>

    <!-- Section Header -->
    <div class="section-header">
        <h2 class="mb-0">Question Details</h2>
    </div>

    <div class="card p-4 mb-3">
        <form action="/admin/exam/<%= examId %>/edit/coding/<%= codingQuestion._id %>" method="POST">
            
            <div class="mb-3">
                <label for="questionTile" class="form-label">Title:</label>
                <input type="text" id="questionTile" name="questionTile" class="form-control" value="<%= codingQuestion.questionTile %>" required>
            </div>

            <div class="mb-3">
                <label for="questiontext" class="form-label">Question:</label>
                <textarea id="questiontext" name="questiontext" class="form-control" required><%= codingQuestion.questiontext %></textarea>
            </div>

            <div class="mb-3">
                <label for="constraints" class="form-label">Constraints:</label>
                <input type="text" id="constraints" name="constraits" class="form-control" value="<%= codingQuestion.constraits %>" required>
            </div>

            <div class="mb-3">
                <label for="inputFormat" class="form-label">Input Format:</label>
                <input type="text" id="inputFormat" name="inputFormat" class="form-control" value="<%= codingQuestion.inputFormat %>" required>
            </div>

            <div class="mb-3">
                <label for="outputFormat" class="form-label">Output Format:</label>
                <input type="text" id="outputFormat" name="outputFormat" class="form-control" value="<%= codingQuestion.outputFormat %>" required>
            </div>

            <div class="mb-3">
                <label for="sampleInput" class="form-label">Sample Input:</label>
                <textarea id="sampleInput" name="sampleInput" class="form-control"  required> <%= codingQuestion.sampleInput %> </textarea>
            </div>
            
            <div class="mb-3">
                <label for="sampleOutput" class="form-label">Sample Output:</label>
                <textarea id="sampleOutput" name="sampleOutput" class="form-control" required><%= codingQuestion.sampleOutput %></textarea>
            </div>

            <div class="mb-3">
                <label for="solutionTemplate" class="form-label">Solution Template:</label>
                <textarea id="solutionTemplate" name="solutionTemplate" class="form-control" required><%= codingQuestion.solutionTemplate %></textarea>
            </div>

            <button type="submit" class="btn btn-primary">Update Coding Question</button>
        </form>
    </div>

    <!-- Delete Form -->
    <div class="text-center">
        <form action="/admin/exam/<%= examId %>/delete/coding/<%= codingQuestion._id %>" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-danger" onclick="return confirm('Delete this question?')">🗑 Delete Question</button>
        </form>
    </div>

    <!-- Back Link -->
    <div class="text-center mt-4">
        <a href="/admin/exam/questions/<%= examId %>" class="back-btn">⬅ Back to Questions</a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
